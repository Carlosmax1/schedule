import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 40,
    padding: 25,
    gap: 20,
  },
  buttonBack: {
    flexDirection: "row",
    gap: 5,
    alignItems: "center",
  },
  textBack: {
    fontFamily: "Roboto_400Regular",
    fontSize: 18,
  },
});

export { styles };
